const fs = require('fs');
let dataTs = fs.readFileSync('src/data.ts', 'utf8');

// The items with category "porcoes" start at id "7" up to id "12", plus molhos id "13", "14", "15"? Wait, molhos are "porcoes"? Let's check.
